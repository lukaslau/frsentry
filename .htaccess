# Prevent directory listing
Options -Indexes

# Deny direct execution of PHP files (PrestaShop loads them via its bootstrap)
<FilesMatch "\.php$">
    Order deny,allow
    Deny from all
</FilesMatch>
